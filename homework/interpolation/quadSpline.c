#include <stdlib.h>
#include <assert.h>

typedef struct { int      n;
                 double*  x;
                 double*  y;
                 double*  b;
                 double*  c; } quadSpline;

quadSpline* quadSplineAlloc( int n, double* x, double* y ){
  
}
